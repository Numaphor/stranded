{
  "version": "0.3.0",
  "configurations": [
    {
      "name": "Launch game (mGBA)",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/scripts/launch_emulator.js",
      "cwd": "${workspaceFolder}"
    }
  ]
}
