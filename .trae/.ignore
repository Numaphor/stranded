# Ignore everything at the repo root (files and directories)
/*

# Ignore root-level hidden files and directories too
/.*

# Explicitly allow only these directories and everything inside them
!/src/
!/src/**

!/include/
!/include/**
