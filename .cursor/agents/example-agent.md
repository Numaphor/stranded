---
name: example-agent
description: Short description shown when the agent is chosen (e.g. "Runs X and returns Y").
---

# Prompt

What this subagent should do when invoked. The parent agent passes context here; the subagent works in its own context window and returns a final message.

- Goal 1
- Goal 2
- Output: what to return (summary, files changed, etc.)
